" Collect all notes in Markdown format under the 'diary' repository
" The 'path' is relative to the location of the 'vimrc', no need to use the
" environment variable $MYVIMRC here.
let g:vimwiki_list = [{'path': '~/.config/vim/diary',
                      \ 'syntax': 'markdown', 'ext': '.md'}]

